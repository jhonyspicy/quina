# Quina AI開発ルール

## プロジェクトの目的

作業開始時に`README.md`と`docs/product-vision.md`を確認する。プロダクト方針に記載した初回リリースの範囲、将来構想、未決定事項、試作版の利用実績を区別し、構想を実装済みの仕様として扱わない。

## ドキュメントの役割

- `docs/architecture/`: 現在採用している設計・現在の正解を記述する。実装済みの構成、責務、データモデル、主要な処理の流れや制約を、現在の実装と一致する状態に保つ。未実装の提案や将来構想を、現在の仕様として記載しない。
- `docs/adr/`: 重要な設計判断とその理由を記録する。背景、検討した選択肢、採用した判断、理由、影響を残す。判断を変更する場合は新しいADRを追加し、以前のADRに後継への参照や置き換え済みの状態を記して、判断の履歴を保つ。
- `openspec/`: これから実装する変更仕様を管理する。今回何を変更するか、要件、受け入れ条件、実装タスクを明確にする。導入済みのOpenSpecの構成・ワークフローがある場合はそれに従い、確定仕様やアーカイブもその規約に沿って維持する。変更履歴だけを現在の設計の唯一の説明にしない。

これらのディレクトリやファイルがまだない場合は、実際の変更で必要になった時点で作成する。未確定の設計を推測して埋めたり、不要な雛形を一括作成したりしない。

## 重要な設計変更・実装時の手順

1. 変更前に、関連する実装、`docs/architecture/`、`docs/adr/`、`openspec/`の既存ファイルを確認する。同じ内容を別の場所に重複して作らず、関連する既存ドキュメントの更新を優先する。
2. これから行う変更の仕様は`openspec/`で整理する。重要な設計判断を伴う場合は、判断と理由を`docs/adr/`に記録する。検討中・採用済み・実装済みを区別する。
3. 重要な設計変更を実装する際は、同じ変更の中で関連する`docs/architecture/`も確認・更新する。変更後の構成、責務、データモデル、外部との契約、運用上の制約など、影響する記述を実装と一致させる。
4. 完了報告やOpenSpecの変更完了・アーカイブの前に、実装・変更仕様・関連Docsの整合性を確認する。矛盾があれば、採用した仕様と実装を確認して解消する。過去の変更仕様をいくつも探さなくても、関連する`docs/architecture/`から現在の設計を理解できる状態にする。
5. 完了報告には、更新したDocsと確認結果を含める。確認の結果、Docsの更新が不要な場合はその理由を簡潔に記す。未解決の不整合がある場合は、完了扱いにせず内容を明示する。

## 記述・編集の原則

- ドキュメントの更新はMarkdownファイルの直接編集でよい。専用スキルの有無にかかわらず、関連Docsの確認・更新を開発作業に含める。
- 現在の設計、判断の理由、変更予定はそれぞれの場所に記し、必要に応じて相互リンクする。同じ説明を複数ファイルに丸ごと複製しない。
- 既存の構成・命名・記述スタイルを尊重する。既存の内容や作業中の変更を不用意に削除・上書きしない。
- 軽微な修正では、設計や仕様への影響に応じて更新範囲を決める。すべての修正でADRの追加や新しい設計資料の作成を必須にしない。
